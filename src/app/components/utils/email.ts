const sendEmail = (to: string, subject: string, body: string) => {
    // Code for sending email using a third-party email service
    // You can integrate with a service like SendGrid or any other email service provider
  
    // Example code for sending email using console log
    console.log(`Sending email to: ${to}`);
    console.log(`Subject: ${subject}`);
    console.log(`Body: ${body}`);
  };
  
  export default sendEmail;
  